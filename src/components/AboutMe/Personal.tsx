import React from "react";
import styled from "styled-components";
import { Box } from "../Shared";

const PersonalBox = styled(Box)`
  animation-duration: 0.5s;
`;

const Personal = () => <PersonalBox>hi</PersonalBox>;

export default Personal;
