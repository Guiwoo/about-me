import React from "react";
import styled from "styled-components";
import { Box } from "../Shared";

const EducationBox = styled(Box)`
  animation-duration: 2s;
`;

const Education = () => <EducationBox>hi</EducationBox>;

export default Education;
